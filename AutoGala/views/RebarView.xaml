<UserControl x:Class="AutoGala.views.RebarView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:AutoGala.views"
             xmlns:common="clr-namespace:AutoGala.Common"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <GroupBox Header="Rebar List Data"
      Grid.Column="2"
      Margin="0,0,10,0">

        <DockPanel>

            <Grid DockPanel.Dock="Bottom">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>

                <Button Content="Add Rebar"
                    Grid.Column="0"
                    Command="{Binding AddRebarCommand}"
                    Height="25"
                    Margin="5"/>

                <Button Content="Delete Rebar"
                    Grid.Column="1"
                    Command="{Binding RemoveRebarCommand}"
                    Height="25"
                    Margin="5"/>

                <Button Content="{Binding EditButtonText}"
                    Grid.Column="2"
                    Command="{Binding EditRebarCommand}"
                    PreviewMouseLeftButtonDown="SaveEditButton_PreviewMouseLeftButtonDown"
                    Height="25"
                    Margin="5"/>

                <Button Content="Paste Rebars"
                    Grid.Column="3"
                    Command="{Binding PasteRebarCommand}"
                    Height="25"
                    Margin="5"/>

                <Button Content="To Gala"
                    Grid.Column="4"
                    Command="{Binding HookToGalaCommand}"
                    Height="25"
                    Margin="5"/>
            </Grid>

            <DataGrid x:Name="RebarGrid"
                  SelectedItem="{Binding SelectedRebar}"
                  ItemsSource="{Binding Rebars}"
                  CopyingRowClipboardContent="CopyingRowClipboardContent"
                  AutoGenerateColumns="False"
                  HeadersVisibility="Column"
                  CanUserAddRows="False"
                  GridLinesVisibility="All"
                  IsReadOnly="{Binding IsGridReadOnly}">


                <DataGrid.Columns>

                    <DataGridTextColumn Header="#"
                                        Width="30"
                                        Binding="{Binding Id}"
                                        IsReadOnly="True"/>

                    <DataGridTextColumn Header="As [cm2]"
                                        Width="*">

                        <DataGridTextColumn.Binding>
                            <Binding Path="Area">
                                <Binding.ValidationRules>
                                    <common:RequiredNumberValidationRule/>
                                </Binding.ValidationRules>
                            </Binding>
                        </DataGridTextColumn.Binding>

                    </DataGridTextColumn>

                    <DataGridTextColumn Header="X [cm]"
                                        Width="*">

                        <DataGridTextColumn.Binding>
                            <Binding Path="X">
                                <Binding.ValidationRules>
                                    <common:RequiredNumberValidationRule/>
                                </Binding.ValidationRules>
                            </Binding>
                        </DataGridTextColumn.Binding>

                    </DataGridTextColumn>

                    <DataGridTextColumn Header="Y [cm]"
                                        Width="*">

                        <DataGridTextColumn.Binding>
                            <Binding Path="Y">
                                <Binding.ValidationRules>
                                    <common:RequiredNumberValidationRule/>
                                </Binding.ValidationRules>
                            </Binding>
                        </DataGridTextColumn.Binding>

                    </DataGridTextColumn>

                </DataGrid.Columns>

            </DataGrid>

        </DockPanel>

    </GroupBox>
</UserControl>
