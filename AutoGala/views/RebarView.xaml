<UserControl x:Class="AutoGala.views.RebarView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:AutoGala.views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    
    <GroupBox Header="Rebar List Data"
          Grid.Column="1"
          Margin="0,0,10,0">

        <DataGrid x:Name="RebarGrid"
              AutoGenerateColumns="False"
              HeadersVisibility="Column"
              CanUserAddRows="False"
              GridLinesVisibility="All"
              IsReadOnly="True">

            <DataGrid.Columns>

                <DataGridTextColumn Header="#"
                                Width="30"
                                Binding="{Binding Id}"/>

                <DataGridTextColumn Header="As [cm2]"
                                Width="*"
                                Binding="{Binding Area}"/>

                <DataGridTextColumn Header="X [cm]"
                                Width="*"
                                Binding="{Binding X}"/>

                <DataGridTextColumn Header="Y [cm]"
                                Width="*"
                                Binding="{Binding Y}"/>
            </DataGrid.Columns>

        </DataGrid>

    </GroupBox>
</UserControl>
